/** @type {import('next').NextConfig} */
const nextConfig = {
  //   crossOrigin: "anonymous",
  compiler: {
    //Enabled styled component
    styledComponents: true,
  },
};

module.exports = nextConfig;
